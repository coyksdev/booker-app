query GetCategories {
  service_category {
    id
    name
    icon
  }
}

query GetShop {
  shop {
    id
    name
    address
    location
    logo
  }
}

mutation UpdateUser($id: uuid!, $input: users_set_input) {
  updateUser(pk_columns: {id: $id}, _set: $input) {
    id
  }
}
